# SMTP Configuration for Quote Form Emails
# Copy this file to .env and fill in your SMTP credentials

# SMTP Server Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_FROM=noreply@krkapower.com
SMTP_USER=your_username
SMTP_PASS=your_password

# Destination email for quote notifications (internal)
QUOTE_TO=[email protected]
